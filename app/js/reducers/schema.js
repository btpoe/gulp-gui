// const { UPDATE } = require('../actions/schema');

const initialState = require('../config');

module.exports = function(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};
